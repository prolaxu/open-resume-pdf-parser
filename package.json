{
  "name": "@prolaxu/open-resume-pdf-parser",
  "version": "0.1.0",
  "description": "Extract and parse resume data from PDF files using OpenResume's parsing logic.",
  "main": "dist/index.cjs",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "test": "vitest"
  },
  "license": "AGPL-3.0",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  },
  "dependencies": {
    "pdfjs-dist": "^5.4.449"
  }
}
